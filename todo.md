1. use template button in photos/[slug], remove text copy this prompt instead for better results instead of copy this prompt use button "use this template" something like this >> DONE!

2. update photos in landing page ( 100+ trusted users ) >> done

3. update tag template page
